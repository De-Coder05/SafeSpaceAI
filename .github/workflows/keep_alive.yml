name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes
  workflow_dispatch: # Allows manual triggering

jobs:
  ping_render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run: curl -I https://safespace-server-7bxn.onrender.com
